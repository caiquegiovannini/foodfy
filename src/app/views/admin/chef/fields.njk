<div class="item">
    <div class="field-title">Nome *</div>
    <input
        type="text"
        name="name"
        value="{{chef.name}}"
    />
</div>

<div class="item">
    <div class="field-title">Avatar *</div>
    {% if chef.path %}
        <div class="avatar-path" id={{chef.file_id}}>
            <span>{{chef.path}}</span>
        </div>
    {% endif %}

    <div class="avatar-upload" id="{{avatar.id}}">
        <input 
            id="avatar-input"
            type="file" 
            name="avatar"
            accept="image/*"
            onload="PhotosUpload.handleFileInput(event)"
        >
        <p>Enviar Avatar</p>
    </div>
    <input type="hidden" name="removed_files">

</div>